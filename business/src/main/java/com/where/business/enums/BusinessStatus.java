package com.where.business.enums;

public enum BusinessStatus {
    PENDING_APPROVAL,  // Awaiting admin approval
    APPROVED,          // Approved and available
    CLOSED,            // Business is permanently closed
    TEMPORARILY_CLOSED // Business is temporarily closed
}
